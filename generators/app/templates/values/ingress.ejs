

ingress:
  enabled: false
  tls: false
  certificate: ''
  issuer:
    name: letsencrypt-staging
  hosts:<% for (var i = 0; i < publicWorkloads.length; i++) { workload = publicWorkloads[i]; %>
    <%- workload.name %>:
      - name: ''
        path: /<% } for (var i = 0; i < databases.length; i++) { database = databases[i]; %>
    <%- database.explorer.name %>:
      - name: ''
        path: /<% } %>