<% if (workloads.length) { -%>
images:<% for (var i = 0; i < workloads.length; i++) { workload = workloads[i]; %>
  <%- _.camelCase(workload.name) %>:
    repository: <%- workload.image.substr(0, workload.image.indexOf(':')) %>
    tag: <%- workload.image.substr(workload.image.indexOf(':') + 1) %><% }} %>