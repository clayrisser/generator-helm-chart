

service:<% for (var i = 0; i < workloads.length; i++) { workload = workloads[i]; %>
  <%- _.camelCase(workload.name) %>:
    type: NodePort
    externalTrafficPolicy: Cluster
    nodePorts:
      http: ''<% } for (var i = 0; i < databases.length; i++) { database = databases[i]; %>
  <%- _.camelCase(database.explorer.name) %>:
    type: NodePort
    externalTrafficPolicy: Cluster
    nodePorts:
      http: ''<% } for (var i = 0; i < databases.length; i++) { database = databases[i]; %>
  <%- _.camelCase(database.name) %>:
    type: ClusterIP
    externalTrafficPolicy: Cluster
    nodePorts:
      <%- _.camelCase(database.name) %>: ''<% } %>