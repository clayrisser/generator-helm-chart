{{- if (and .Values.ingress.enabled (and .Values.config.mysql.internal .Values.config.mysql.phpmyadmin)) }}
{{- $host := (index .Values.ingress.hosts.phpmyadmin 0) }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "<%- name %>.fullname" . }}-phpmyadmin
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
    {{- if (empty .Values.ingress.certificate) }}
    certmanager.k8s.io/cluster-issuer: {{ .Values.ingress.issuer.name }}
    kubernetes.io/tls-acme: 'true'
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
  labels:
    app: {{ template "<%- name %>.name" . }}-phpmyadmin
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  rules:
    - host: {{ $host.name }}
      http:
        paths:
          - path: {{ $host.path }}
            backend:
              serviceName: {{ template "<%- name %>.fullname" $ }}-phpmyadmin
              servicePort: http
  {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
  tls:
    - secretName: {{ template "<%- name %>.certificate" . }}
      hosts:
        - {{ $host.name }}
  {{- end }}
{{- end }}
