{{- if (and .Values.config.elasticsearch.internal .Values.config.kibana.enabled) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-kibana
  labels:
    app: {{ template "<%- _.kebabCase(name) %>.name" . }}-kibana
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  {{- if .Values.ingress.kibana.enabled }}
  type: ClusterIP
  {{- else }}
  type: {{ .Values.service.kibana.type }}
  {{- if (or (eq .Values.service.kibana.type "LoadBalancer") (eq .Values.service.kibana.type "NodePort")) }}
  externalTrafficPolicy: {{ .Values.service.kibana.externalTrafficPolicy }}
  {{- end }}
  {{- end }}
  ports:
    - name: http
      port: {{ (and (eq .Values.service.kibana.type "LoadBalancer") (not (empty .Values.service.kibana.ports.http))) | ternary .Values.service.kibana.ports.http "80" }}
      targetPort: container
      {{- if (and (eq .Values.service.kibana.type "NodePort") (not (empty .Values.service.kibana.ports.http))) }}
      nodePort: {{ .Values.service.kibana.ports.http }}
      {{- end }}
  selector:
    app: {{ template "<%- _.kebabCase(name) %>.name" . }}-kibana
    release: {{ .Release.Name }}
{{- end }}
