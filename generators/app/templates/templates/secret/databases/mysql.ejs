<% if (_.find(databases, { name: 'mysql'})) { %>
  {{- $mysql := .Values.config.mysql }}
  mysql_password: {{ $mysql.password | quote }}
  mysql_url: {{ (include "<%- _.kebabCase(name) %>.mysql-url" .) | quote }}<% } %>