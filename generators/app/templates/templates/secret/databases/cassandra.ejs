<% if (_.find(databases, { name: 'cassandra'})) { %>
  {{- $cassandra := .Values.config.cassandra }}
  cassandra_url: {{ $cassandra.url | b64enc }}
  cassandra_password: {{ $cassandra.password | b64enc }}<% } %>