{{- if (and .Values.config.postgres.internal (not (and (eq .Values.config.postgres.terminationPolicy "Pause") .Values.config.postgres.pause))) }}
apiVersion: kubedb.com/v1alpha1
kind: Postgres
metadata:
  name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
  labels:
    app: {{ template "<%- _.kebabCase(name) %>.name" . }}-postgres
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  version: {{ .Values.config.postgres.version | quote }}
  configSource:
    configMap:
      name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
  standbyMode: {{ .Values.config.postgres.standbyMode | quote }}
  streamingMode: asynchronous
  leaderElection:
    leaseDurationSeconds: 15
    renewDeadlineSeconds: 10
    retryPeriodSeconds: 2
  databaseSecret:
    secretName: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres-auth
  {{- if .Values.config.postgres.prometheus }}
  monitor:
    agent: prometheus.io/builtin
  {{- end }}
  {{- if (gt (.Values.config.postgres.replicas | int64) 1) }}
  replicas: {{ .Values.config.postgres.replicas }}
  {{- end }}
  {{- if .Values.persistence.enabled }}
  storageType: Durable
  storage:
    {{- if .Values.persistence.storageClass }}
    {{- if (eq "-" .Values.persistence.storageClass) }}
    storageClassName: ''
    {{- else }}
    storageClassName: {{ .Values.persistence.storageClass }}
    {{- end }}
    {{- end }}
    accessModes:
      - {{ .Values.persistence.accessMode }}
    resources:
      requests:
        storage: {{ .Values.persistence.size }}
  {{- else }}
  storageType: Ephemeral
  {{- end }}
  init:
    scriptSource:
      configMap:
        name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres-init
  podTemplate:
    {{- if .Values.persistence.velero.enabled }}
    metadata:
      annotations:
        backup.velero.io/backup-volumes: datadir
    {{- end }}
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: group
                    operator: In
                    values:
                      - dedicated
      resources:
{{ toYaml .Values.config.resources | indent 8 }}
  serviceTemplate:
    metadata:
      annotations:
        passMe: ToService
    spec:
      type: {{ .Values.service.postgres.type }}
      {{- if (or (eq .Values.service.postgres.type "LoadBalancer") (eq .Values.service.postgres.type "NodePort")) }}
      externalTrafficPolicy: {{ .Values.service.postgres.externalTrafficPolicy }}
      {{- end }}
      ports:
        - name: postgres
          port: {{ (and (eq .Values.service.postgres.type "LoadBalancer") (not (empty .Values.service.postgres.ports.http))) | ternary .Values.service.postgres.ports.http "5432" }}
          targetPort: postgres
          {{- if (and (eq .Values.service.postgres.type "NodePort") (not (empty .Values.service.postgres.ports.postgres))) }}
          nodePort: {{ .Values.service.postgres.ports.postgres }}
          {{- end }}
  replicaServiceTemplate:
    annotations:
      passMe: ToReplicaService
    spec:
      type: {{ .Values.service.postgres.type }}
      {{- if (or (eq .Values.service.postgres.type "LoadBalancer") (eq .Values.service.postgres.type "NodePort")) }}
      externalTrafficPolicy: {{ .Values.service.postgres.externalTrafficPolicy }}
      {{- end }}
      ports:
        - name: postgres
          port: 5432
          targetPort: postgres
          {{- if (and (eq .Values.service.postgres.type "NodePort") (not (empty .Values.service.postgres.ports.postgres))) }}
          nodePort: {{ .Values.service.postgres.ports.postgres }}
          {{- end }}
  updateStrategy:
    type: {{ (eq .Values.config.updateStrategy "Recreate") | ternary "RollingUpdate" .Values.config.updateStrategy }}
  terminationPolicy: {{ .Values.config.postgres.terminationPolicy }}
{{- if .Values.persistence.stash.enabled }}
---
apiVersion: appcatalog.appscode.com/v1alpha1
kind: AppBinding
metadata:
  name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
  labels:
    app: {{ template "<%- _.kebabCase(name) %>.name" . }}-postgres
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: kubedb.com/postgres
  version: {{ .Values.config.postgres.version | quote }}
  clientConfig:
    service:
      name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
      path: /
      port: 5432
      query: sslmode=disable
      scheme: postgresql
  secretTransforms:
    - renameKey:
        from: POSTGRES_USER
        to: username
    - renameKey:
        from: POSTGRES_PASSWORD
        to: password
{{- if (and .Values.persistence.stash.enabled (not (empty .Values.persistence.stash.restore.snapshot.postgres))) }}
---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
  labels:
    app: {{ template "<%- _.kebabCase(name) %>.name" . }}-postgres
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    kubedb.com/kind: Postgres
spec:
  driver: Restic
  task:
    name: postgres-restore-{{ index .Values.config.postgres.versionMap .Values.config.postgres.version }}
  repository:
    name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
  target:
    ref:
      apiVersion: appcatalog.appscode.com/v1alpha1
      kind: AppBinding
      name: {{ template "<%- _.kebabCase(name) %>.fullname" . }}-postgres
  rules:
    - snapshots:
      - {{ .Values.persistence.stash.restore.snapshot.postgres | quote }}
{{- end }}
{{- end }}
{{- end }}
