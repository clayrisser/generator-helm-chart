<% if (publicWorkloads.length || databases.length) { %>

# Services and Load Balancing
ingress:<% for (var i = 0; i < publicWorkloads.length; i++) { workload = publicWorkloads[i]; %>
  - variable: ingress.<%- _.snakeCase(workload.name) %>.enabled
    default: true
    description: ''
    type: boolean
    label: '<%- workload.name %> ingress enabled'
    show_subquestion_if: true
    group: 'Services and Load Balancing'
    subquestions:
      - variable: ingress.<%- _.snakeCase(workload.name) %>.tls
        default: false
        description: ''
        type: boolean
        required: true
        label: '<%- workload.name %> ingress tls'
      - variable: ingress.<%- _.snakeCase(workload.name) %>.hostname
        default: xip.io
        description: ''
        type: hostname
        required: true
        label: '<%- workload.name %> ingress hostname'
      - variable: ingress.<%- _.snakeCase(workload.name) %>.path
        default: /
        description: ''
        type: string
        required: true
        label: '<%- workload.name %> ingress path'
      - variable: ingress.<%- _.snakeCase(workload.name) %>.issuer.name
        default: letsencrypt-staging
        description: ''
        type: enum
        show_if: ingress.<%- _.snakeCase(workload.name) %>.tls=true
        options:
          - none
          - letsencrypt-staging
          - letsencrypt-prod
        required: true
        label: '<%- workload.name %> ingress issuer name'
      - variable: ingress.<%- _.snakeCase(workload.name) %>.certificate
        default: ''
        description: ''
        type: string
        show_if: ingress.<%- _.snakeCase(workload.name) %>.tls=true&&ingress.<%- _.snakeCase(workload.name) %>.issuer.name=none
        required: false
        label: '<%- workload.name %> ingress certificate'
  - variable: service.<%- _.snakeCase(workload.name) %>.type
    default: NodePort
    description: ''
    type: enum
    show_if: ingress.<%- _.snakeCase(workload.name) %>.enabled=false
    options:
      - ClusterIP
      - NodePort
      - LoadBalancer
    required: true
    label: '<%- workload.name %> service type'
    show_subquestion_if: NodePort
    group: 'Services and Load Balancing'
    subquestions:
      - variable: service.<%- _.snakeCase(workload.name) %>.nodePorts.http
        default: ''
        description: ''
        type: int
        min: 30000
        max: 32767
        show_if: ingress.<%- _.snakeCase(workload.name) %>.enabled=false
        label: '<%- workload.name %> http port'<% } for (var i = 0; i < databases.length; i++) { database = databases[i]; %>
  - variable: ingress.<%- _.snakeCase(database.explorer.name) %>.enabled
    default: true
    description: ''
    type: boolean
    show_if: config.<%- _.snakeCase(database.explorer.name) %>.enabled=true
    label: '<%- database.explorer.name %> ingress enabled'
    show_subquestion_if: true
    group: 'Services and Load Balancing'
    subquestions:
      - variable: ingress.<%- _.snakeCase(database.explorer.name) %>.tls
        default: false
        description: ''
        type: boolean
        show_if: config.<%- _.snakeCase(database.explorer.name) %>.enabled=true
        required: true
        label: '<%- database.explorer.name %> ingress tls'
      - variable: ingress.<%- _.snakeCase(database.explorer.name) %>.hostname
        default: xip.io
        description: ''
        type: hostname
        show_if: config.<%- _.snakeCase(database.explorer.name) %>.enabled=true
        required: true
        label: '<%- database.explorer.name %> ingress hostname'
      - variable: ingress.<%- _.snakeCase(database.explorer.name) %>.path
        default: /
        description: ''
        type: string
        show_if: config.<%- _.snakeCase(database.explorer.name) %>.enabled=true
        required: true
        label: '<%- database.explorer.name %> ingress path'
      - variable: ingress.<%- _.snakeCase(database.explorer.name) %>.issuer.name
        default: letsencrypt-staging
        description: ''
        type: enum
        show_if: config.<%- _.snakeCase(database.explorer.name) %>.enabled=true&&ingress.<%- _.snakeCase(database.explorer.name) %>.tls=true
        options:
          - none
          - letsencrypt-staging
          - letsencrypt-prod
        required: true
        label: '<%- database.explorer.name %> ingress issuer name'
      - variable: ingress.<%- _.snakeCase(database.explorer.name) %>.certificate
        default: ''
        description: ''
        type: string
        show_if: config.<%- _.snakeCase(database.explorer.name) %>.enabled=true&&ingress.<%- _.snakeCase(database.explorer.name) %>.tls=true&&ingress.<%- _.snakeCase(database.explorer.name) %>.issuer.name=none
        required: false
        label: '<%- database.explorer.name %> ingress certificate'
  - variable: service.<%- _.snakeCase(database.explorer.name) %>.type
    default: NodePort
    description: ''
    type: enum
    show_if: ingress.<%- _.snakeCase(database.explorer.name) %>.enabled=false&&config.<%- _.snakeCase(database.explorer.name) %>.enabled=true
    options:
      - ClusterIP
      - NodePort
      - LoadBalancer
    required: true
    label: '<%- database.explorer.name %> service type'
    show_subquestion_if: NodePort
    group: 'Services and Load Balancing'
    subquestions:
      - variable: service.<%- _.snakeCase(database.explorer.name) %>.nodePorts.http
        default: ''
        description: ''
        type: int
        min: 30000
        max: 32767
        show_if: ingress.<%- _.snakeCase(database.explorer.name) %>.enabled=false
        label: '<%- database.explorer.name %> port'<% } for (var i = 0; i < databases.length; i++) { database = databases[i]; %>
  - variable: service.<%- _.snakeCase(database.name) %>.type
    default: ClusterIP
    description: ''
    type: enum
    options:
      - ClusterIP
      - NodePort
      - LoadBalancer
    required: true
    label: '<%- database.name %> service type'
    show_subquestion_if: NodePort
    group: 'Services and Load Balancing'
    subquestions:
      - variable: service.<%- _.snakeCase(database.name) %>.nodePorts.<%- _.snakeCase(database.name) %>
        default: ''
        description: ''
        type: int
        min: 30000
        max: 32767
        label: '<%- database.name %> port'<% }} %>